## Problem

<!-- What maintainer uncertainty or user problem does this change address? -->

## Change

<!-- Describe the smallest complete change. -->

## Evidence

- [ ] Tests passed (`npm test`)
- [ ] Lint passed (`npm run lint`)
- [ ] Typecheck passed (`npm run typecheck`)
- [ ] Build passed (`npm run build`)
- [ ] npm package allowlist passed (`npm run package:check`)
- [ ] Packed-install E2E passed when distribution behavior changed (`npm run test:e2e`)
- [ ] MergeReceipt report reviewed (`node dist/cli.js verify`)

## Compatibility and security

- [ ] JSON/config compatibility is preserved or versioned and documented.
- [ ] New command, git, output, token, and untrusted-PR risks were considered.
- [ ] Linux, Windows, and macOS path/process behavior was considered.
- [ ] Documentation describes actual implemented behavior only.
