# Hosted pull request acceptance

This documentation-only pull request exercises MergeReceipt's real
`pull_request` workflow against `main`. It verifies hosted base-revision
discovery, shallow-checkout handling, read-only permissions, GitHub Step
Summary output, and bundled Action dogfooding.

The pull request is acceptance evidence only after its hosted checks finish;
the existence of this file alone is not evidence that those checks passed.
